{
  "name": "gem-id-vn",
  "version": "1.0.0",
  "type": "module",
  "description": "UnlimitMail.com Cloudflare Workers API + UI",
  "main": "dist/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "build": "esbuild src/worker.ts --bundle --platform=neutral --target=es2022 --outfile=dist/worker.js",
    "format": "prettier --write \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "typescript": "^5.4.5",
    "prettier": "^3.2.5",
    "wrangler": "^3.52.1"
  }
}
